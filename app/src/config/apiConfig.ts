/**
 * ChronoForge Global API Configuration
 * 
 * DEVELOPER NOTE: 
 * For physical Android devices, 'localhost' will NOT work.
 * You must use the local IPv4 address of your computer (e.g., 192.168.x.x).
 * Ensure both the device and the computer are on the SAME Wi-Fi network.
 */

export const API_CONFIG = {
    // Current Local Development IP
    BASE_URL: 'http://172.31.35.50:3000',

    // Timeouts and other network settings can go here
    TIMEOUT: 10000,
};
